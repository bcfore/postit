<% header = link_to('All Posts', posts_path) + " >> #{@category.name}".html_safe  %>
<%= render 'shared/content_title', title: header %>

<%= render partial: "posts/post", collection: @category.posts %>

